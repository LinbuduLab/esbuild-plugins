import { snowpackPluginSvgrImport } from './snowpack-plugin-svgr-import';

describe('snowpackPluginSvgrImport', () => {
  it('should work', () => {
    expect(snowpackPluginSvgrImport()).toEqual('snowpack-plugin-svgr-import');
  });
});
