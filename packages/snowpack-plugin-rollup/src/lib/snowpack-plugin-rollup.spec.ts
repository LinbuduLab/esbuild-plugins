import { snowpackPluginRollup } from './snowpack-plugin-rollup';

describe('snowpackPluginRollup', () => {
  it('should work', () => {
    expect(snowpackPluginRollup()).toEqual('snowpack-plugin-rollup');
  });
});
