# rollup-plugin-static-serve

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test rollup-plugin-static-serve` to execute the unit tests via [Jest](https://jestjs.io).
