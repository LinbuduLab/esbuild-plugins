import { snowpackPluginGql } from './snowpack-plugin-gql';

describe('snowpackPluginGql', () => {
  it('should work', () => {
    expect(snowpackPluginGql()).toEqual('snowpack-plugin-gql');
  });
});
