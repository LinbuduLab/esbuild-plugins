import { snowpackPluginYamlImport } from './snowpack-plugin-yaml-import';

describe('snowpackPluginYamlImport', () => {
  it('should work', () => {
    expect(snowpackPluginYamlImport()).toEqual('snowpack-plugin-yaml-import');
  });
});
