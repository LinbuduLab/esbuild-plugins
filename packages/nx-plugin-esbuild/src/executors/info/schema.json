{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Info executor",
  "description": "",
  "type": "object",
  "properties": {
    "buildTarget": {
      "type": "string",
      "description": "The target to run to build you the app"
    },
    "serveTarget": {
      "type": "string",
      "description": "The target to run to serve you the app"
    }
  },
  "required": []
}
