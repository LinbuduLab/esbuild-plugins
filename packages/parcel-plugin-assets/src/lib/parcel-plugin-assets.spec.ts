import { parcelPluginAssets } from './parcel-plugin-assets';

describe('parcelPluginAssets', () => {
  it('should work', () => {
    expect(parcelPluginAssets()).toEqual('parcel-plugin-assets');
  });
});
