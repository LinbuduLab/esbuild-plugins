import { snowpackPluginSwcCompiler } from './snowpack-plugin-swc-compiler';

describe('snowpackPluginSwcCompiler', () => {
  it('should work', () => {
    expect(snowpackPluginSwcCompiler()).toEqual('snowpack-plugin-swc-compiler');
  });
});
