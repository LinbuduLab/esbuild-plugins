import { snowpackPluginAssets } from './snowpack-plugin-assets';

describe('snowpackPluginAssets', () => {
  it('should work', () => {
    expect(snowpackPluginAssets()).toEqual('snowpack-plugin-assets');
  });
});
